---
- name: Install Kubernetes Dashboard
  shell: kubectl create -f ../infrastructure/kubernetes-dashboard/deployment.yml

- name: Add user and cluster role
  shell: kubectl create -f ../infrastructure/kubernetes-dashboard/rbac.yml